#include <iostream>
#include <vector>
#include <string>
#include <list>
using namespace std;



int main()
{
    srand(time(0));
    setlocale(LC_CTYPE, "UKR");

    /*list<int> l = { 3, 5, 9, 10, -2, 3, 5 };*/
    /*l.push_back(10);
    l.push_front(10);
    l.pop_front();
    l.pop_back();*/

    /*l.clear();*/

    /*list<int>::iterator it = l.begin();
    
    advance(it, 5);

    l.insert(it, 1000);
    l.erase(it);

    for (int el : l) {
        cout << el << " ";
    }*/


    list<string> l;

    l.cend();



    return 0;
}
